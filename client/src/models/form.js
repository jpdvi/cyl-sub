function form(numWindow, windowTime, endTime, deviceId){
  this.numWindow = numWindow; // -> int
  this.windowTime = windowTime; // -> int
  this.endTime = endTime; // -> int
  this.deviceId = deviceId; // -> String
}

// TODO: add type validation through inheritance.
